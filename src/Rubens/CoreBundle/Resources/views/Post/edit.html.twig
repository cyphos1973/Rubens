{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Post edit</h1>
{% for type in ['info', 'success', 'error'] %}
        {% for flashMessage in app.session.flashbag.get(type) %}
            <div class="alert alert-{{ type }}">
                {{ flashMessage }}
            </div>
        {% endfor %}
    {% endfor %}
    {{ form(edit_form) }}
            {#     <form role="form" action="" method="post" {{ form_enctype(edit_form) }}>

                                    {{ form_errors(edit_form) }}

                                    <div class="form-group">
                                      {{ form_label(edit_form.title, "Titre de l'article") }}
                                      {{ form_errors(edit_form.title) }}
                                      {{ form_widget(edit_form.title) }}
                                    </div>
                                    <div class="form-group">
                                      {{ form_label(edit_form.chapo, "Chapo de l'article") }}
                                      {{ form_errors(edit_form.chapo) }}
                                      {{ form_widget(edit_form.chapo) }}
                                    </div>
                                    <div class="form-group">
                                      {{ form_label(edit_form.body, "Contenu de l'article") }}
                                      {{ form_errors(edit_form.body) }}
                                      {{ form_widget(edit_form.body) }}
                                    </div>
                                    <div class="form-group">
                                      {{ form_label(edit_form.createdAt, "Date de l'article") }}
                                      {{ form_errors(edit_form.createdAt) }}
                                      {{ form_widget(edit_form.createdAt) }}
                                    </div>
                                    {{ form_rest(edit_form) }}
                                    </form>#}
        <ul class="record_actions">
    <li>
        <a href="{{ path('post') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
