{# src/Rubens/RubensWebBundle/Resources/views/Backend/Image/index.html.twig #}

{% extends "RubensWebBundle:Backend:backend.html.twig" %}
{% block body %}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                TABLES : images
            </div>
            <!-- /.panel-heading -->
            {% for flashMessage in app.session.flashbag.get('notice') %}
            <div class="alert alert-success">
                <div class="flash-notice">
                    {{ flashMessage }}
                </div>
            </div>
            {% endfor %}
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-tables">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Url</th>
                                <th>Alt</th>
                                <th>Crée</th>
                                <th>Mis à jour</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for image in entities %}
                            <tr class="odd gradeX">
                                <td>{{ image.id }}</td>
                                <td>{{ image.url }}</td>
                                <td>{{ image.alt }}</td>
                                <td>{{ image.createdAt|date('d-m-Y à H:i')}}</td>
                                <td>{{ image.updatedAt|date('d-m-Y à H:i')}}</td>
                                <td><a href="{{path('rubens_admin_image_edit', {'id': image.id} )}}">Modifier</a> - <a href="{{path('rubens_admin_image_show', {'id': image.id} )}}">Voir</a></td>
                            </tr>
                           {% endfor %}
                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
{% endblock %}
