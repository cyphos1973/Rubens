{# src/Rubens/RubensWebBundle/Resources/views/Backend/Video/index.html.twig #}

{% extends "RubensWebBundle:Backend:backend.html.twig" %}
{% block body %}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                TABLES : vidéos
            </div>
            <!-- /.panel-heading -->
            {% for flashMessage in app.session.flashbag.get('notice') %}
            <div class="alert alert-success">
                <div class="flash-notice">
                    {{ flashMessage }}
                </div>
            </div>
            {% endfor %}
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-tables">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Titre</th>
                                <th>Crée</th>
                                <th>Mis à jour</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for video in entities %}
                            <tr class="odd gradeX">
                                <td>{{ video.id }}</td>
                                <td>{{ video.title }}</td>
                                <td>{{ video.createdAt|date('d-m-Y à H:i')}}</td>
                                <td>{{ video.updatedAt|date('d-m-Y à H:i')}}</td>
                                <td><a href="{{path('rubens_admin_video_edit', {'id': video.id} )}}">Modifier</a> - <a href="{{path('rubens_admin_video_show', {'id': video.id} )}}">Voir</a></td>
                            </tr>
                           {% endfor %}
                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
{% endblock %}
